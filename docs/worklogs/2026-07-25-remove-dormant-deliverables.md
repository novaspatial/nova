# Close #13 as already-live; remove the dormant deliverables pipeline

Goal: settle S17 (admin file download, #13) and stop carrying infrastructure nothing uses. Investigating #13 showed its stems half has been live for a while — `UploadManager` renders a download button for studio (`allowDownload={isStudio}` → the studio-only `stemDownloadRoute`, with "studio sees all" RLS beneath) — and its deliverables half has nothing to download: no UI anywhere consumed the `deliverables` table, the `project-deliverables` bucket, or their 4 API routes, and the `'deliverable'` value in `project_files.file_type` was unwritable (the files route accepts only `stem|master_ref|mix`). Delivery in practice is mix-type `project_files` on the Listen page. Onur's call: close #13 as already satisfied and destroy the dead shape.

The removal (`90c1f82`, −868 lines) deletes the deliverables route subtree and strips the `'deliverable'` kind from the #35 seam (`bucketFor`/`tableFor`/`createUpload`/`deliverableDownloadRoute`), `uploadRunner`, `projectCleanup`, `requireProjectChild`, and the portal types. It also removes the PATCH `deliverable_format` side-write — a latent silent no-op: `deliverables` never had an UPDATE RLS policy, so the request-scoped update always matched 0 rows. `signedDownload` now always forces the stored file name. Lint clean; 817 tests in 69 files pass.

The DB half is `20260725_remove_deliverables.sql`, verified empty on the remote first (0 rows, 0 objects, 0 tagged files) and guard-aborting if a replay ever finds data: drop the table (its 3 RLS policies go with it), drop the 4 `project-deliverables` policies on `storage.objects`, delete the bucket row, and narrow `project_files_file_type_check` to the 3 real values. Applied post-deploy so the old GET join never queries a missing table.

One ripple for #27: D7's recorded purge scope says "stems and deliverables both" — with the table gone, "deliverables" maps to mix-type `project_files`, so the purge in practice covers stem + mix rows via `projectCleanup`. Noted on #27 for confirmation at pickup; D7 itself (Jamie's ruling) stands unamended. The glossary keeps **Deliverable** as the business term for the signed-off mix files.
