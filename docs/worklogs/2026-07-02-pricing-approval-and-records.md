Pricing approval — module finalization and tracker records

Date: 2026-07-02. Phase: Faz 1–2 kapanışı

The admins answered. Mike and Jamie both approved the per-song pricing proposal, which unparked the finalization work that had been waiting since June 26. Mike also settled the policy half of the tax decision: Canadian clients are charged GST, PST is not charged, and the calculation mechanism is left to us. Jamie confirmed his reading of the algorithm — guard rails that stop stacked discounts from cutting below the price floor — and added a new request: the price calculator should be the start of the new-project flow and should also live on the front page as an interactive widget, replacing the static call-to-action so visitors can price out a project and see their welcome discount in action.

With approval in hand, the pricing module was finalized exactly as the parked plan specified. Two defensive fixes went in: percentage codes are clamped to a valid zero-to-one-hundred range before stacking, so a negative code can never inflate the price above list (previously a minus-ten-percent code priced a single song above the list price), and add-ons are de-duplicated so a repeated entry can never charge the customer twice. The test file grew from three legacy checks to a forty-four-test suite covering the bulk-tier boundaries, percent and fixed codes against the cap and the floor, private codes suppressing the bulk tier, add-on ordering and de-duplication, defensive edges, and a four-hundred-and-five-point invariant grid asserting the breakdown lines always sum, the floor always holds, and every money field stays in integer cents. Lint, the full four-hundred-sixty-one-test suite, and the type-check baseline all stayed clean.

The tracker was then brought in line with reality. The decisions issue now records the approval, the partial tax decision, and a standalone open question for Mike: HST provinces fold GST and PST into a single harmonized rate, so "GST yes, PST no" is ambiguous there — five percent GST or the full HST needs confirming before the checkout can show a tax line. The pricing-module issues were closed against the finalization commits, with the pure-math issue retitled from CAD to USD floor per the earlier currency decision and an honest note that one-code-per-order is guaranteed structurally by the input type rather than by a dedicated test. Two stale issues whose work had shipped in June — per-post SEO metadata and the auto-generated share image — were closed to match the devplan. Jamie's calculator request became its own tracked issue, noting the shared quote component it must eventually split with the checkout slice, the missing number-stepper and select primitives, and its dependency on the still-open welcome-percentage decision.

The agent-facing docs were refreshed alongside: the CI notes now reflect that the workflow runs the test suite, and the known-gaps warning no longer claims the Project type is unsynced or the sitemap unbuilt — the real remaining gap is that the live checkout still charges the flat legacy price until the checkout slice rewires it to the new module.
