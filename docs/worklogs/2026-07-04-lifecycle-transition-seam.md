# 2026-07-04 — Lifecycle transition seam (#34)

Gave `workflow.ts` the transition half of the lifecycle it was missing: `canTransition(from, to, actor)` over a forward-only role-aware table (system moves `pending_payment→uploading`, client `uploading→in_review`, studio the rest — 12 legal edges, no self-loops, nothing targets `pending_payment` or legacy `processing`), plus `canUploadStems`/`canUploadMix`, `isProjectStatus`, and a single `NOTIFIABLE_STATUSES` source that `projectNotifications` now derives from. `canonicalStatus` is the one place that knows `processing ≡ mixing`. Onur triaged the 2 judgment calls: the nonsense `in_review→review` edge is gone (Send-for-Review no longer renders there), and studio keeps `uploading→in_review` as an API-only force-advance.

That closed the 2 live holes. `finish-upload` now pre-reads the project (404 for foreign ids, was a silent 200), no-ops when already `in_review`, rejects everything else with 400, and writes with a compare-and-swap on the read status — so it can no longer drag a `delivered` project back and email "we received your files". The PATCH route fetches `id, status`, validates the transition before the `approved` deliverables side-write, and CASes the update (0 rows → 409). The webhook and payment-status poll consult the same guard but always record `paid_at`; a late webhook can't regress an advanced project anymore. The files routes, upload page, UploadManager, and DeliverBanner all read the module — 5 drifting status arrays and the `studioCanUploadMix` prop deleted, and their tests pass unmodified as the regression proof.

The RLS floor needed backing too: the client UPDATE policy is column-agnostic, so any client could set `status` via PostgREST directly — including `pending_payment→uploading`, i.e. stem uploads without paying. `20260705_harden_status_writes.sql` (applied) adds a freeze-style trigger in the `20260702` pattern: service and studio pass, clients only `uploading→in_review`. Verified live with a simulated client JWT: the illegal jump raises `42501`, rolled back. 2 pre-existing holes stay open as follow-ups: clients can still PostgREST-INSERT a born-`uploading` row with forged `paid_at` (the dev-bypass insert makes a naive INSERT fence wrong), and the payment-status poll's claim write has been dead since `20260702` (its client-session `paid_at` write trips the order-fields trigger).

Suite is 580 tests green (102 in `workflow.test.ts`, incl. an exhaustive 9×9×3 sweep pinning the table), lint and build clean. #24 (order email) and #27 (purge) can now read `NOTIFIABLE_STATUSES` and the `delivered` transition point instead of growing new arrays.
